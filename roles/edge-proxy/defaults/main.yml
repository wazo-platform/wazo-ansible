---
kamailio_app: 'APP_EDP'
edp_tls_on: False
kamailio_sip_domain: 'openstack.wazo.community'
kamailio_db_engine: 'PGSQL' # check Kamailio doc for supported DB engine (PGSQL)
kamailio_db_host: '127.0.0.1'
kamailio_db_port: 5432 # default for PGSQL engine
kamailio_db_root_user: 'posrgtes'
kamailio_db_root_pass: 'superpass' # root user password of DB
kamailio_db_name: 'edp'
kamailio_db_user: 'kamailio'
kamailio_db_pass: 'password'
kamailio_db_user_ro: 'kamailioro' # readonly user
kamailio_db_pass_ro: 'password' # readonly pass
sbc_install_conf: true
sbc_conf_dir: '/etc/kamailio'
sbc_config_files:
    - kamailio.cfg
    - kamailio-local.cfg
    - dispatcher.list
    - kamctlrc
kamailio_debug: 2
sipr_servers:
     - 127.0.0.2
sbc_servers:
    - 127.0.0.2
rtp_engine_servers:
    - udp:127.0.0.1:2223=1 # protocol:ip_address:port=1  multiple rtproxies for LB with weights (missing weight defaults to 1)
