- job:
    name: wazo-ansible-install
    description: install Wazo platform
    pre-run: pre.yaml
    run: run.yaml

- project:
    check:
      jobs:
        - wazo-ansible-install:
            nodeset: debian9-vm
